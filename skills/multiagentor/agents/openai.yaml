interface:
  display_name: "MultiAgentor"
  short_description: "Route and guide MultiAgentor browser, task, and RPA workflows"
  default_prompt: "Use $multiagentor to self-check and update the skill and npm CLI, load only the references needed for my request, guide the required choices, supervise execution when started, collect results, and give me a safe repeat-run prompt."
